[project]
name = "elo-ai"
version = "0.1.0"
description = "eLo AI — Creative Personality Operating System"
requires-python = ">=3.10"
dependencies = ["anthropic>=0.25.0"]

[project.scripts]
elo = "main:main"

[tool.pytest.ini_options]
testpaths = ["tests"]

"""
core/ — eLo AI engine package.

Modules:
    behavior_engine  — deterministic response simulation (grounding, imagination, contradiction)
    memory_engine    — persistent JSON store, active influence signals, export
    core_engine      — runtime orchestration (mode, project, memory → response)
    project_engine   — project registry and namespace management
    state_engine     — conversational state tracking (mode, turn, emotion arc)
"""
